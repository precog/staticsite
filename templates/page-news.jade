extends layout-alt

block prepend title
  | #{ page.title } - 

block content

  .holder
    h1.page-title= page.title
    .two-columns
      include author
      //- underscore helps us filter and sort our articles
      - var articles = _.chain(contents.about.news._.directories).map(function(item) {
      -   // map articles to index pages of subdirectories of 'articles'
      -   return item.index
      - }).compact().sortBy(function(item) { // compact removes any directories w/o an index
      -   // sort them by date
      -   return -item.date
      - }).first(5).value() // and finally take the first 5
      each article in articles
        article.news-item.dark-background
          header
            h2= article.title
              // a(href=article.url)= article.title
            h3
              mixin author(article.metadata.author)
          section.content!= article.intro
          if article.hasMore
            p.read-more
              a(href=article.url) read more
    .two-columns-end
    .clear-left